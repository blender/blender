/* SPDX-FileCopyrightText: 2025 Blender Authors
 *
 * SPDX-License-Identifier: GPL-2.0-or-later */

/**
 * File replaced by the content passed to `GPUShaderCreateInfo.typedef_source(source)` through the
 * Python API. Otherwise, it is replaced by an empty string.
 */

#pragma once
/* This file must replaced at runtime. The following content is only a possible implementation. */
#pragma runtime_generated

/* Some struct def */
struct MyType {
  int foo;
  float bar;
};
