diff --git a/source/CMakeLists.txt b/source/CMakeLists.txt
index 5c6dda9e8..b2202a600 100755
--- a/source/CMakeLists.txt
+++ b/source/CMakeLists.txt
@@ -115,7 +115,9 @@ else()
 endif()
 
 if(UNIX)
-    list(APPEND PLATFORM_LIBS pthread)
+    if(NOT ANDROID)
+        list(APPEND PLATFORM_LIBS pthread)
+    endif()
     find_library(LIBRT rt)
     if(LIBRT)
         list(APPEND PLATFORM_LIBS rt)
@@ -409,6 +411,9 @@ int main() { return 0; }")
     if(ENABLE_PIC)
         list(APPEND ARM_ARGS -DPIC -fPIC)
     endif()
+    if(CMAKE_CXX_COMPILER_TARGET)
+        list(APPEND ARM_ARGS --target=${CMAKE_CXX_COMPILER_TARGET})
+    endif()
     add_definitions(${ARM_ARGS})
     if(FPROFILE_GENERATE)
         if(INTEL_CXX)
@@ -937,10 +942,14 @@ if(X265_LATEST_TAG OR NOT GIT_FOUND)
     foreach(LIB ${CMAKE_CXX_IMPLICIT_LINK_LIBRARIES} ${PLATFORM_LIBS})
         if(IS_ABSOLUTE ${LIB} AND EXISTS ${LIB})
             list(APPEND PLIBLIST "${LIB}")
+        elseif(LIB MATCHES "^-")
+            # Already a linker flag
+            list(APPEND PLIBLIST "${LIB}")
         else()
             list(APPEND PLIBLIST "-l${LIB}")
         endif()
     endforeach()
+    list(REMOVE_DUPLICATES PLIBLIST)
     if(PLIBLIST)
         # blacklist of libraries that should not be in Libs.private
         list(REMOVE_ITEM PLIBLIST "-lc" "-lpthread" "-lmingwex" "-lmingwthrd"
